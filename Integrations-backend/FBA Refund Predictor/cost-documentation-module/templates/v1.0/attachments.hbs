<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Supporting Attachments - {{anomaly_type}}</title>
    <link rel="stylesheet" href="../_styles.css">
</head>
<body class="page-attachments">
    <div class="header">
        <h2>Supporting Attachments</h2>
        <p class="page-number">Page 4 of 5</p>
    </div>

    <div class="content">
        <div class="attachments-overview">
            <h3>Evidence Attachments</h3>
            <p>This page contains thumbnails and access information for all supporting documents, screenshots, and evidence files related to this anomaly.</p>
        </div>

        <div class="attachments-grid">
            {{#each attachments}}
            <div class="attachment-item">
                <div class="attachment-header">
                    <h4>{{name}}</h4>
                    <span class="attachment-type">{{type}}</span>
                </div>
                
                {{#if thumbnail_url}}
                <div class="attachment-thumbnail">
                    <img src="{{thumbnail_url}}" alt="Thumbnail for {{name}}" class="thumbnail-image">
                </div>
                {{else}}
                <div class="attachment-placeholder">
                    <div class="placeholder-icon">{{type_icon}}</div>
                    <span class="placeholder-text">{{type}} File</span>
                </div>
                {{/if}}
                
                <div class="attachment-details">
                    <p><strong>File Size:</strong> {{file_size}}</p>
                    <p><strong>Upload Date:</strong> {{upload_date}}</p>
                    {{#if description}}
                    <p><strong>Description:</strong> {{description}}</p>
                    {{/if}}
                    {{#if tags}}
                    <p><strong>Tags:</strong> 
                        {{#each tags}}
                        <span class="tag">{{this}}</span>
                        {{/each}}
                    </p>
                    {{/if}}
                </div>
                
                <div class="attachment-actions">
                    <a href="{{download_url}}" class="download-link" target="_blank">
                        Download {{type}}
                    </a>
                    {{#if preview_url}}
                    <a href="{{preview_url}}" class="preview-link" target="_blank">
                        Preview
                    </a>
                    {{/if}}
                </div>
            </div>
            {{/each}}
        </div>

        <div class="evidence-links">
            <h3>External Evidence Links</h3>
            {{#if external_links}}
            <div class="links-list">
                {{#each external_links}}
                <div class="external-link">
                    <h4>{{title}}</h4>
                    <p>{{description}}</p>
                    <a href="{{url}}" class="external-link-url" target="_blank">
                        {{url}}
                    </a>
                    <span class="link-type">{{link_type}}</span>
                </div>
                {{/each}}
            </div>
            {{else}}
            <p>No external evidence links available for this anomaly.</p>
            {{/if}}
        </div>

        <div class="data-exports">
            <h3>Data Exports</h3>
            {{#if data_exports}}
            <div class="exports-list">
                {{#each data_exports}}
                <div class="data-export">
                    <h4>{{export_name}}</h4>
                    <p>{{export_description}}</p>
                    <div class="export-metadata">
                        <span><strong>Format:</strong> {{format}}</span>
                        <span><strong>Records:</strong> {{record_count}}</span>
                        <span><strong>Generated:</strong> {{generated_date}}</span>
                    </div>
                    <a href="{{export_url}}" class="export-download" target="_blank">
                        Download {{format}} Export
                    </a>
                </div>
                {{/each}}
            </div>
            {{else}}
            <p>No data exports available for this anomaly.</p>
            {{/if}}
        </div>

        <div class="verification-notes">
            <h3>Verification Notes</h3>
            <p>All attachments have been verified for authenticity and relevance to this anomaly. Each file contains metadata that can be used to verify its integrity and origin.</p>
            <ul class="verification-list">
                <li>File hashes verified against original uploads</li>
                <li>Timestamps preserved from source systems</li>
                <li>Access logs maintained for audit purposes</li>
                <li>Digital signatures applied where applicable</li>
            </ul>
        </div>
    </div>

    <div class="footer">
        <p>Report ID: {{report_id}} | {{attachments.length}} attachments included</p>
    </div>
</body>
</html>






