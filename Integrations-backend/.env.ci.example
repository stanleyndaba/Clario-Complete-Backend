# Minimal env for CI integration tests (mocks or sandbox)
JWT_SECRET=ci-secret
TOKEN_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef

# Amazon sandbox or mocked gateway
AMAZON_CLIENT_ID=ci-client-id
AMAZON_CLIENT_SECRET=ci-client-secret
AMAZON_REDIRECT_URI=http://localhost/callback
AMAZON_MARKETPLACE_ID=ATVPDKIKX0DER
AMAZON_REGION=us-east-1

# Disable WS during CI unless explicitly enabled
ENABLE_WS=false

# Supabase (optional for CI; if set, persistence test will run real insert)
SUPABASE_URL=
SUPABASE_ANON_KEY=
DATABASE_URL=

