import * as dotenv from 'dotenv'; dotenv.config(); import { supabaseAdmin } from './src/database/supabaseClient'; async function run() { const { data, error } = await supabaseAdmin.from('evidence_sources').select('*').in('user_id', ['demo-user', '00000000-0000-0000-0000-000000000000']); console.log('evidence_sources for demo-user:', JSON.stringify(data, null, 2)); if(error) console.error(error); } run();
