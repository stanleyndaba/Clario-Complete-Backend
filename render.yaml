services:
  - type: web
    name: opside-main-api
    env: python
    buildCommand: |
      pip install --upgrade pip
      pip cache purge
      pip install -r requirements.txt
    startCommand: uvicorn src.app:app --host 0.0.0.0 --port 
    healthCheckPath: /health
